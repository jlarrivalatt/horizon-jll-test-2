Use this command to open dev preview

``` shopify theme dev --store=jll-test-2 ```

Use this command to push dev to store

``` shopify theme push --store=jll-test-2 ```

Use this command to pull existing store data

``` shopify theme pull --store=jll-test-2 ```